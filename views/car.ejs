<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Car Details</title>

    <%- include('./partials/head') %>
</head>
<body>
    <%- include('./partials/header') %>

    <main>
        <section class="car-details">
            <img src="<%= car.image %>" alt="<%= car.year + ' ' + car.brand + car.model %>" class="details-img">
            <h2><%= car.year + ' ' + car.brand + ' ' + car.model %></h2>
            <h2><%= car.hp %> Horsepower</h2>

            <div class="edit-delete-buttons">
                <a href="/car/<%= car.id %>/edit">Edit</a>
                <form action="/car/<%=car._id%>/delete?_method=DELETE" method="POST">
                    <button class="delete-button">Delete</button>
                </form>
            </div>
        </section>
        
    </main>
    
</body>
</html>